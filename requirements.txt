# ── UI ────────────────────────────────────────────────────────────────────────
customtkinter>=5.2.0
pillow>=10.0.0

# ── Hotkeys & clipboard ───────────────────────────────────────────────────────
keyboard>=0.13.5
mouse>=0.7.1           # macro recorder mouse events
pyperclip>=1.8.2
pywin32>=306          # win32clipboard (used by core/typer.py)
pynput>=1.7.6          # macro recorder keyboard/mouse capture & replay

# ── System tray ───────────────────────────────────────────────────────────────
pystray>=0.19.5

# ── Whiteboard (pywebview / Edge WebView2) ───────────────────────────────────
# whiteboard.py embeds the offline JS bundle in an Edge WebView2 window.
# pythonnet is the Windows backend pywebview uses to bridge to the WinForms
# WebView2 control.
pywebview>=5.0
pythonnet>=3.0.3; sys_platform == "win32"

# ── Text-refine providers ─────────────────────────────────────────────────────
groq>=0.4.0
cerebras-cloud-sdk>=1.0.0
httpx>=0.25.0
certifi>=2024.0.0
truststore>=0.9.0       # makes Python trust OS cert store → AV/corporate SSL works

# ── Speech-to-text (Whisper) ──────────────────────────────────────────────────
faster-whisper>=1.0.0
sounddevice>=0.4.6
numpy>=1.24.0
onnxruntime>=1.16.0
noisereduce>=3.0.0

# ── Screen / GIF recording ────────────────────────────────────────────────────
av>=12.0.0             # PyAV — FFmpeg bindings for screen recorder & GIF encoder
psutil>=5.9.0          # process utilities (used by screen recorder)

# ── Spell check ──────────────────────────────────────────────────────────────
pyspellchecker>=0.7.0

# ── Translation (screenshot "Translate (Google)" menu item) ─────────────────
deep-translator>=1.11.4   # scrapes Google Translate web endpoint, free + no API key

# ── Offline OCR (screenshot "Translate (Offline Arabic OCR)" menu item) ─────
# Needs system Tesseract binary + ara.traineddata in <app>/tessdata/.
# See PROJECT.md "Tesseract Arabic OCR" section for dist bundling notes.
pytesseract>=0.3.10
opencv-python-headless>=4.8.0   # OCR image preprocessing (denoise / deskew / threshold)

# ── Optional: local Qwen GGUF (heavy — skip for cloud-only builds) ────────────
# llama-cpp-python>=0.2.0
# huggingface-hub>=0.20.0
python-bidi
arabic-reshaper
